# Codex prompt patches

No Codex prompt patches are currently applied. Put named exact-match transformations here;
profile computation applies them before appends and fails when the source text no longer
matches.
