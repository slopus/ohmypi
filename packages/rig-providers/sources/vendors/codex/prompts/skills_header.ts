export const skills_header =
    "<skills_instructions>\n" +
    "## Skills\n" +
    "A skill is a set of instructions provided through a `SKILL.md` source. Below is the " +
    "list of skills that can be used. Each entry includes a name, description, and source " +
    "locator. `file` locators are on the host filesystem, `environment resource` locators " +
    "are owned by an execution environment, `orchestrator resource` locators are opaque " +
    "non-filesystem resources, and `custom resource` locators use their provider's access " +
    "mechanism.\n" +
    "### Available skills\n";
